<nav class="navbar navbar-expand-lg navbar-light bg-warning pt-0 mb-3">
  <div class="container">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <% if user_signed_in? %>
        <div class="navbar-nav mr-auto">
          <% if @hunt %>
            <%= link_to 'Homes', hunt_homes_path(@hunt), class: 'nav-item nav-link' %>
            <%= link_to 'Add Home', new_hunt_home_path(@hunt), class: 'nav-item nav-link' %>
          <% end %>
        </div>
        <div class="navbar-nav">
          <%= link_to 'Switch Hunt', hunts_path, class: 'nav-item nav-link' %>
          <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'nav-item nav-link' %>
        </div>
      <% end %>
    </div>
  </div>
</nav>
